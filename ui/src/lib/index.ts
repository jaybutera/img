// place files you want to import through the `$lib` alias in this folder.
import {
    handle_file_upload,
    get_image_names,
    img_server,
    get_tags,
    get_challenge,
} from '$lib/img.ts';
